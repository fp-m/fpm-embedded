//
// Initialization of bindings for loader.
//
#define FPM_BIND(name) { #name, (void*) name }

    { "", NULL },

    // Kernel routines.
    FPM_BIND(fpm_alloc),
    FPM_BIND(fpm_alloc_dirty),
    FPM_BIND(fpm_delay_msec),
    FPM_BIND(fpm_delay_usec),
    FPM_BIND(fpm_editline),
    FPM_BIND(fpm_exec),
    FPM_BIND(fpm_free),
    FPM_BIND(fpm_get_datetime),
    FPM_BIND(fpm_get_dotw),
    FPM_BIND(fpm_getchar),
    FPM_BIND(fpm_getkey),
    FPM_BIND(fpm_getopt),
    FPM_BIND(fpm_getwch),
    FPM_BIND(fpm_heap_available),
    FPM_BIND(fpm_print_version),
    FPM_BIND(fpm_printf),
    FPM_BIND(fpm_putchar),
    FPM_BIND(fpm_puts),
    FPM_BIND(fpm_putwch),
    FPM_BIND(fpm_realloc),
    FPM_BIND(fpm_reboot),
    FPM_BIND(fpm_set_datetime),
    FPM_BIND(fpm_shell),
    FPM_BIND(fpm_sizeof),
    FPM_BIND(fpm_snprintf),
    FPM_BIND(fpm_sscanf),
    FPM_BIND(fpm_stack_available),
    FPM_BIND(fpm_strlcpy),
    FPM_BIND(fpm_strlcpy_from_utf8),
    FPM_BIND(fpm_strlcpy_to_utf8),
    FPM_BIND(fpm_strlcpy_unicode),
    FPM_BIND(fpm_strtod),
    FPM_BIND(fpm_strtol),
    FPM_BIND(fpm_strtoul),
    FPM_BIND(fpm_strwlen),
    FPM_BIND(fpm_time_usec),
    FPM_BIND(fpm_tokenize),
    FPM_BIND(fpm_truncate),
    FPM_BIND(fpm_utf8len),
    FPM_BIND(fpm_vprintf),
    FPM_BIND(fpm_vsnprintf),
    FPM_BIND(fpm_vsscanf),
    FPM_BIND(fpm_wputs),

    // FIlesystem routines.
    FPM_BIND(f_chdir),
    FPM_BIND(f_chdrive),
    FPM_BIND(f_chmod),
    FPM_BIND(f_close),
    FPM_BIND(f_closedir),
    FPM_BIND(f_eof),
    FPM_BIND(f_error),
    FPM_BIND(f_expand),
    FPM_BIND(f_findfirst),
    FPM_BIND(f_findnext),
    FPM_BIND(f_forward),
    FPM_BIND(f_getcwd),
    FPM_BIND(f_getdrive),
    FPM_BIND(f_getlabel),
    FPM_BIND(f_gets),
    FPM_BIND(f_lseek),
    FPM_BIND(f_mkdir),
    FPM_BIND(f_mkfs),
    FPM_BIND(f_mount),
    FPM_BIND(f_open),
    FPM_BIND(f_opendir),
    FPM_BIND(f_printf),
    FPM_BIND(f_putc),
    FPM_BIND(f_puts),
    FPM_BIND(f_read),
    FPM_BIND(f_readdir),
    FPM_BIND(f_rename),
    FPM_BIND(f_setlabel),
    FPM_BIND(f_size),
    FPM_BIND(f_sizeof_directory_t),
    FPM_BIND(f_sizeof_file_t),
    FPM_BIND(f_stat),
    FPM_BIND(f_statfs),
    FPM_BIND(f_strerror),
    FPM_BIND(f_sync),
    FPM_BIND(f_tell),
    FPM_BIND(f_truncate),
    FPM_BIND(f_unlink),
    FPM_BIND(f_unmount),
    FPM_BIND(f_utime),
    FPM_BIND(f_write),

    // Standard C library.
    FPM_BIND(atof),
    FPM_BIND(memcmp),
    FPM_BIND(memmove),
    FPM_BIND(memset),
    FPM_BIND(stpcpy),
    FPM_BIND(stpncpy),
    FPM_BIND(strcasecmp),
    FPM_BIND(strcat),
    FPM_BIND(strchr),
    FPM_BIND(strcmp),
    FPM_BIND(strcpy),
    FPM_BIND(strlen),
    FPM_BIND(strncmp),
    FPM_BIND(strrchr),
