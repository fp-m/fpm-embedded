FP/M release 1.0.

Use HELP to see a list of built-in commands.

For details on any command, run it with -h option.
For example:

    flash:/ > mount -h
    Usage:
        mount [sd: | flash:]

When SD card is present, run MOUNT SD: to enable it.
Command MOUNT without arguments shows a list of
active filesystems.
